# PostgreSQL
#spring.datasource.url=jdbc:postgresql://localhost:5432/swp391_hackathon
#spring.datasource.username=postgres
#spring.datasource.password=12345
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Resend - QUAN TRỌNG: đổi 2 dòng này
spring.mail.host=smtp.gmail.com
spring.mail.port=587
#spring.mail.username=mtung638@gmail.com
#spring.mail.password=zddu oiwb hmls kwhc
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Base URL
#app.base-url=http://localhost:8080
app.base-url=${APP_BASE_URL}
server.port=${PORT:8080}

# Swagger
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/api-docs


# Tắt warning security
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration